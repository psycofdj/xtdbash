#!bin/bash

function lastdir_save() {
  envloader_run || true
  pwd > ${HOME}/.lastdir
}

function lastdir_load() {
  local l_val=$(cat ${HOME}/.lastdir 2>/dev/null)
  if [[ -d "$l_val" ]]; then
      cd $l_val
  fi
}

decorate_builtin cd "" lastdir_save
lastdir_load
